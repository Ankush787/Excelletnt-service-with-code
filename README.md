# Excelletnt-service-with-code
here is code 
Before executing the project, make sure the database is properly configured:

Create a MySQL database named excellent.

Inside this database, create the 3 tables exactly as defined in the code provided.

After the database and tables are created successfully, the project will run without errors.

⚠ This project is created strictly for educational and learning purposes only.

Ankush
